[
  {
    "before": "      const cameraLayoutDebugEnabled = isCameraLayoutDebugEnabled();\n      // TEMPORARY: URL-only (?cameraLayoutDebug=1) detector/distance\n      // diagnostics — a bounded rolling sample (latest 50), never\n      // persisted, never fed back into detection/classification. See\n      // recordDetectorSample below for exactly what it records.\n",
    "after": "      const cameraLayoutDebugEnabled = isCameraLayoutDebugEnabled();\n      // TEMPORARY: URL-only (?cameraLayoutDebug=1) detector/distance\n      // diagnostics — a bounded rolling sample (latest 300), never\n      // persisted, never fed back into detection/classification. See\n      // recordDetectorSample below for exactly what it records.\n"
  },
  {
    "before": "      // width/height and the pre-existing boxClipped edge-touch flag —\n      // never real landmark points, never pixels, never biometric\n      // identity data — into a bounded rolling buffer (latest 50).\n      // Exists purely to prove, on a real device, whether TinyFaceDetector\n      // itself returns no result at close range, or returns a result the\n",
    "after": "      // width/height and the pre-existing boxClipped edge-touch flag —\n      // never real landmark points, never pixels, never biometric\n      // identity data — into a bounded rolling buffer (latest 300).\n      // Exists purely to prove, on a real device, whether TinyFaceDetector\n      // itself returns no result at close range, or returns a result the\n"
  },
  {
    "before": "      // extended for. Never read by classifyFeatures/onComplete/rec/any\n      // production decision.\n      const recordDetectorSample = (partial) => {\n        if (!cameraLayoutDebugEnabled) return;\n        if (detectorDebugStartRef.current == null) detectorDebugStartRef.current = performance.now();\n        const sample = { elapsedMs: Math.round(performance.now() - detectorDebugStartRef.current), ...partial };\n        detectorSamplesRef.current = [...detectorSamplesRef.current, sample].slice(-50);\n        setDetectorDebugLatest(sample);\n      };\n",
    "after": "      // extended for. Never read by classifyFeatures/onComplete/rec/any\n      // production decision.\n      // Issue B: at most 5 detector ticks/second; 300 samples retain ~60 seconds.\n      // Only geometry/settings are read. Nothing here samples image content.\n      const pendingDetectorSampleRef = useRef(null);\n      const recordDetectorSample = (partial) => {\n        if (!cameraLayoutDebugEnabled) return;\n        pendingDetectorSampleRef.current = partial;\n      };\n      const flushDetectorSample = (geometry, decision) => {\n        if (!cameraLayoutDebugEnabled || !pendingDetectorSampleRef.current) return;\n        const partial = pendingDetectorSampleRef.current;\n        pendingDetectorSampleRef.current = null;\n        if (detectorDebugStartRef.current == null) detectorDebugStartRef.current = performance.now();\n        const sample = {\n          elapsedMs: Math.round(performance.now() - detectorDebugStartRef.current), ...partial,\n          ...geometry, ...decision, boxNearEdge: partial.boxClipped,\n        };\n        const previous = detectorSamplesRef.current.at(-1);\n        if (!previous || JSON.stringify([previous.hasFace, previous.stageKey, previous.hintKey, previous.rejectionReasons]) !==\n          JSON.stringify([sample.hasFace, sample.stageKey, sample.hintKey, sample.rejectionReasons])) {\n          cameraLayoutDebugRef.current?.('detector state transition');\n        }\n        detectorSamplesRef.current = [...detectorSamplesRef.current, sample].slice(-300);\n        setDetectorDebugLatest(sample);\n      };\n"
  },
  {
    "before": "        markTiming('T5_first_frame_eligible');\n        busyRef.current = true;\n        try {\n          const canvas = procCanvasRef.current;\n",
    "after": "        markTiming('T5_first_frame_eligible');\n        busyRef.current = true;\n        let diagnosticGeometry = null;\n        const diagnosticDecision = {};\n        const decideStage = key => { if (cameraLayoutDebugEnabled) diagnosticDecision.stageKey = key; setStageKey(key); };\n        const decideHint = key => { if (cameraLayoutDebugEnabled) diagnosticDecision.hintKey = key; setHintKey(key); };\n        try {\n          const canvas = procCanvasRef.current;\n"
  },
  {
    "before": "          console.log('[LSA] FRAME RECEIVED', totalFramesRef.current, canvas.width + 'x' + canvas.height);\n\n          markTiming('T6_first_inference_begins');\n          const det = await faceapi.detectSingleFace(canvas, new faceapi.TinyFaceDetectorOptions({ inputSize: 320, scoreThreshold: 0.5 })).withFaceLandmarks();\n",
    "after": "          console.log('[LSA] FRAME RECEIVED', totalFramesRef.current, canvas.width + 'x' + canvas.height);\n\n          if (cameraLayoutDebugEnabled) {\n            const layout = readCameraLayoutSnapshot(video, containerRef.current, overlayCanvasRef.current);\n            const cover = layout.previewCoverGeometry;\n            diagnosticGeometry = {\n              videoWidth: video.videoWidth, videoHeight: video.videoHeight,\n              processingCanvasWidth: canvas.width, processingCanvasHeight: canvas.height,\n              previewContainerWidth: layout.container?.rect.width ?? null,\n              previewContainerHeight: layout.container?.rect.height ?? null,\n              previewCanvasWidth: overlayCanvasRef.current?.width ?? null,\n              previewCanvasHeight: overlayCanvasRef.current?.height ?? null,\n              coverScale: cover?.scale ?? null,\n              visibleSourceFraction: cover ? cover.effectiveVisibleWidthFraction * cover.effectiveVisibleHeightFraction : null,\n              mediaTrackSettings: layout.mediaTrackSettings,\n            };\n          }\n          markTiming('T6_first_inference_begins');\n          const det = await faceapi.detectSingleFace(canvas, new faceapi.TinyFaceDetectorOptions({ inputSize: 320, scoreThreshold: 0.5 })).withFaceLandmarks();\n"
  },
  {
    "before": "            if (!withinGrace) {\n              lastDetRef.current = { hasFace: false, ts: now };\n              if (hadFaceRef.current) { diagStageKey = 'stageLost'; setPhase('lost'); setStageKey('stageLost'); }\n              else { diagStageKey = 'stageSearching'; setPhase('searching'); setStageKey('stageSearching'); }\n              hadFaceRef.current = false;\n              setHintKey(null);\n            }\n            recordDetectorSample({\n",
    "after": "            if (!withinGrace) {\n              lastDetRef.current = { hasFace: false, ts: now };\n              if (hadFaceRef.current) { diagStageKey = 'stageLost'; setPhase('lost'); decideStage('stageLost'); }\n              else { diagStageKey = 'stageSearching'; setPhase('searching'); decideStage('stageSearching'); }\n              hadFaceRef.current = false;\n              decideHint(null);\n            }\n            recordDetectorSample({\n"
  },
  {
    "before": "              boxX: null, boxY: null, boxWidth: null, boxHeight: null, boxClipped: null,\n              canvasWidth: canvas.width, canvasHeight: canvas.height, rejectionReasons: ['no_detection'],\n              stageKey: diagStageKey, hintKey: null,\n            });\n            setProgress(Math.min(1, bufferRef.current.length / MIN_STABLE_FRAMES));\n",
    "after": "              boxX: null, boxY: null, boxWidth: null, boxHeight: null, boxClipped: null,\n              canvasWidth: canvas.width, canvasHeight: canvas.height, rejectionReasons: ['no_detection'],\n              stageKey: diagStageKey, hintKey: withinGrace ? hintKey : null,\n            });\n            setProgress(Math.min(1, bufferRef.current.length / MIN_STABLE_FRAMES));\n"
  },
  {
    "before": "            console.log('[LSA] FRAME REJECTED: NaN in metrics');\n            const diagStageKey = hasHadValidFrameRef.current ? 'stageRealigning' : 'stageFaceDetected';\n            setPhase('adjust'); setStageKey(diagStageKey); setHintKey('hintBlurry');\n            recordDetectorSample({\n              hasFace: true, detectorScore: det.detection.score, faceRatio: det.detection.box.width / Math.max(canvas.width, 1),\n",
    "after": "            console.log('[LSA] FRAME REJECTED: NaN in metrics');\n            const diagStageKey = hasHadValidFrameRef.current ? 'stageRealigning' : 'stageFaceDetected';\n            setPhase('adjust'); decideStage(diagStageKey); decideHint('hintBlurry');\n            recordDetectorSample({\n              hasFace: true, detectorScore: det.detection.score, faceRatio: det.detection.box.width / Math.max(canvas.width, 1),\n"
  },
  {
    "before": "            const diagStageKey = hasHadValidFrameRef.current ? 'stageRealigning' : 'stageFaceDetected';\n            const diagHintKey = boxClipped ? 'hintCenterFace' : (REASON_MESSAGES[quality.reasons[0]] || null);\n            setPhase('adjust'); setStageKey(diagStageKey);\n            setHintKey(diagHintKey);\n            recordDetectorSample({\n              hasFace: true, detectorScore: det.detection.score, faceRatio: det.detection.box.width / Math.max(canvas.width, 1),\n",
    "after": "            const diagStageKey = hasHadValidFrameRef.current ? 'stageRealigning' : 'stageFaceDetected';\n            const diagHintKey = boxClipped ? 'hintCenterFace' : (REASON_MESSAGES[quality.reasons[0]] || null);\n            setPhase('adjust'); decideStage(diagStageKey);\n            decideHint(diagHintKey);\n            recordDetectorSample({\n              hasFace: true, detectorScore: det.detection.score, faceRatio: det.detection.box.width / Math.max(canvas.width, 1),\n"
  },
  {
    "before": "          console.log('[LSA] STABLE FRAMES', count, '/', MIN_STABLE_FRAMES);\n          setProgress(Math.min(1, count / MIN_STABLE_FRAMES));\n          setHintKey(null);\n\n          if (count <= 2) { setPhase('aligning'); setStageKey('stageAligning'); bumpRail(railSegmentForPhase('aligning')); return; }\n          if (count <= 7) { setPhase('tracking'); setStageKey('stageTracking'); bumpRail(railSegmentForPhase('tracking')); return; }\n          if (count <= 11) { setPhase('mapping'); setStageKey('stageMapping'); bumpRail(railSegmentForPhase('mapping')); return; }\n          if (count <= 14) {\n            if (phaseRef.current !== 'eyelids') { isolationStartRef.current = now; tagEventsRef.current.push({ label: t('tagTilt', langRef.current), ts: now }); }\n            setPhase('eyelids'); setStageKey('stageEyelids'); bumpRail(railSegmentForPhase('eyelids')); return;\n          }\n          if (count <= 16) {\n            if (phaseRef.current !== 'iris') tagEventsRef.current.push({ label: t('tagLid', langRef.current), ts: now });\n            setPhase('iris'); setStageKey('stageIris'); bumpRail(railSegmentForPhase('iris')); return;\n          }\n          if (count < MIN_STABLE_FRAMES) {\n",
    "after": "          console.log('[LSA] STABLE FRAMES', count, '/', MIN_STABLE_FRAMES);\n          setProgress(Math.min(1, count / MIN_STABLE_FRAMES));\n          decideHint(null);\n\n          if (count <= 2) { setPhase('aligning'); decideStage('stageAligning'); bumpRail(railSegmentForPhase('aligning')); return; }\n          if (count <= 7) { setPhase('tracking'); decideStage('stageTracking'); bumpRail(railSegmentForPhase('tracking')); return; }\n          if (count <= 11) { setPhase('mapping'); decideStage('stageMapping'); bumpRail(railSegmentForPhase('mapping')); return; }\n          if (count <= 14) {\n            if (phaseRef.current !== 'eyelids') { isolationStartRef.current = now; tagEventsRef.current.push({ label: t('tagTilt', langRef.current), ts: now }); }\n            setPhase('eyelids'); decideStage('stageEyelids'); bumpRail(railSegmentForPhase('eyelids')); return;\n          }\n          if (count <= 16) {\n            if (phaseRef.current !== 'iris') tagEventsRef.current.push({ label: t('tagLid', langRef.current), ts: now });\n            setPhase('iris'); decideStage('stageIris'); bumpRail(railSegmentForPhase('iris')); return;\n          }\n          if (count < MIN_STABLE_FRAMES) {\n"
  },
  {
    "before": "              dataFlowStartRef.current = now;\n            }\n            setPhase('symmetry'); setStageKey('stageSymmetry'); bumpRail(railSegmentForPhase('symmetry')); return;\n          }\n\n",
    "after": "              dataFlowStartRef.current = now;\n            }\n            setPhase('symmetry'); decideStage('stageSymmetry'); bumpRail(railSegmentForPhase('symmetry')); return;\n          }\n\n"
  },
  {
    "before": "          console.log('[LSA] STABILITY CHECK', stability.stable ? 'STABLE' : 'NOT STABLE', stability.variance);\n          if (!stability.stable) {\n            setPhase('stabilizing'); setStageKey('stageStabilizing'); bumpRail(railSegmentForPhase('stabilizing'));\n            return;\n          }\n",
    "after": "          console.log('[LSA] STABILITY CHECK', stability.stable ? 'STABLE' : 'NOT STABLE', stability.variance);\n          if (!stability.stable) {\n            setPhase('stabilizing'); decideStage('stageStabilizing'); bumpRail(railSegmentForPhase('stabilizing'));\n            return;\n          }\n"
  },
  {
    "before": "          tagEventsRef.current.push({ label: t('tagSymmetry', langRef.current), ts: now });\n\n          doneRef.current = true;\n          markTiming('T9_analysis_stages_begin');\n          setPhase('finalizing'); setStageKey('stageCalculating'); setHintKey(null);\n          bumpRail(railSegmentForPhase('finalizing'));\n          if (loopRef.current) clearInterval(loopRef.current);\n",
    "after": "          tagEventsRef.current.push({ label: t('tagSymmetry', langRef.current), ts: now });\n\n          // Issue B only: keep camera, sampling and COPY JSON alive through recovery.\n          if (cameraLayoutDebugEnabled) { decideStage('stageComplete'); decideHint(null); return; }\n          doneRef.current = true;\n          markTiming('T9_analysis_stages_begin');\n          setPhase('finalizing'); decideStage('stageCalculating'); decideHint(null);\n          bumpRail(railSegmentForPhase('finalizing'));\n          if (loopRef.current) clearInterval(loopRef.current);\n"
  },
  {
    "before": "          if (debugAvailable) console.log('[LSA] RESULT GENERATED', rec.diagnostics);\n\n          setStageKey('stageComplete');\n          setTimeout(() => onCompleteRef.current(rec), 1000);\n        } catch (e) {\n",
    "after": "          if (debugAvailable) console.log('[LSA] RESULT GENERATED', rec.diagnostics);\n\n          decideStage('stageComplete');\n          setTimeout(() => onCompleteRef.current(rec), 1000);\n        } catch (e) {\n"
  },
  {
    "before": "          if (loopRef.current) { clearInterval(loopRef.current); loopRef.current = null; }\n          doneRef.current = true;\n          setStageKey('stageScanError'); setPhase('error'); setHintKey('hintRestartScan');\n        } finally {\n          busyRef.current = false;\n        }\n",
    "after": "          if (loopRef.current) { clearInterval(loopRef.current); loopRef.current = null; }\n          doneRef.current = true;\n          decideStage('stageScanError'); setPhase('error'); decideHint('hintRestartScan');\n        } finally {\n          flushDetectorSample(diagnosticGeometry, diagnosticDecision);\n          busyRef.current = false;\n        }\n"
  }
]
